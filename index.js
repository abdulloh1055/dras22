let ism = prompt("ismingizni kriting"); // husan
let harf = prompt("birorta harf kriting"); // t
let orni = ism.indexOf(harf); // -1

if (orni === -1) {
    alert("Bu " + ism + " ismda " + harf + " mavjud emas");
} else {
    aler("Bu " + ism + "ismda " + harf + "mavjud")
}